<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.upc.epsevg.passarllista">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.PantallaInicial"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/AppTheme.NoActionBar">
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AfegirAlumne"></activity>
        <activity android:name=".activities.AfegirAssignatura"></activity>
        <activity android:name=".activities.Matriculats"></activity>
        <activity android:name=".activities.PassarLlista"></activity>
        <activity android:name=".activities.HistoricAssistencies"></activity>
        <activity android:name=".activities.AfegirGrup"></activity>

    </application>

</manifest>